name: CI
on:
  workflow_dispatch:
  schedule:
    - cron: '55 15 * * 3'

jobs:
  send-discord-message:
    runs-on: ubuntu-latest
    steps:
    - name: Send CURL
      run: |
        curl -X POST $NEWBIES_DISCORD_WEBHOOK -d '{"content": "Dzisiaj środa"}'
